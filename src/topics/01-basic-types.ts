const name: string = 'Strider';
//name = 1212; Not allowed
let hpPoints: number | 'UNIQUE STRING' | boolean

hpPoints = 34;
hpPoints = 'UNIQUE STRING';
hpPoints = true;

console.log(name, hpPoints);

export {};